const z = {
  object: () => {
    const chain = {
      extend: () => chain,
      merge: () => chain,
      min: () => chain,
      max: () => chain,
      optional: () => chain,
      safeParse: () => ({ success: true }),
    };
    return chain;
  },
  number: () => {
    const chain = {
      min: () => chain,
      max: () => chain,
      optional: () => chain,
    };
    return chain;
  },
  string: () => {
    const chain = {
      optional: () => chain,
    };
    return chain;
  },
};
module.exports = { z };
